@model Tuple<Project, List<Skill>>
@{

    Layout = "_Layout";
    ViewData["Title"] = Model.Item1.Name;
}
    <script>
        function post(c) {
            alert("I have not yet done this part, lol")
            window.location.href = '@Url.Action("Index", "Posts")';
        }
        
    </script>
    <div class="container ">
        <div class="row" style="margin-top: 2%; margin-bottom: 2%">
            <div class="col-2 offset-md-10">
                
                <button type="button" class="btn btn-dark" onclick="post()">REQUEST TO JOIN</button>
            </div>
        </div>


        <h1 class="text-center" style="text-transform:uppercase; font-family: Impact, Charcoal, sans-serif">@Model.Item1.Name</h1>
        <div class="container p-3 mb-2 bg-dark text-white" style="border-radius:1rem !important">
            <h4>DESCRIPTION:</h4>
            <div class="row">
                <div class="col">
                    <p>@Model.Item1.Description</p>
                </div>
            </div>
            <h4>PREFERED SKILLS:</h4>
            <div class="row">
                <div class="col">
                    @if (Model.Item2 != null)
                    {
                        <ul>
                            @foreach (var skill in Model.Item2)
                            {
                                <li>@skill.SkillName</li>
                            }
                        </ul>
                    }
                    else
                    {
                        <h3>No preffered skills for this project</h3>
                    }
                </div>
            </div>
            <h4>TOPICS:</h4>
            <div class="row">
                <div class="col">
                    <p>AREAS OF INTEREST...Wher is this at??</p>
                </div>
            </div>
        </div>
        
     
        
        


    </div>


